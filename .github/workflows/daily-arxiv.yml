name: Daily arXiv scan

on:
  schedule:
    - cron: "0 16 * * *"   # UTC 16:00 = 北京时间 00:00（GitHub cron 可能有延迟）
  workflow_dispatch:

permissions:
  contents: write
  issues: write

concurrency:
  group: arxiv-scan
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - name: Fetch arXiv candidates
        id: fetch
        run: |
          git log -1 --format='Running commit: %H'
          python3 -u scripts/fetch_arxiv.py
      - name: Create issue with candidates
        if: steps.fetch.outputs.has_new == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh label create "new-papers" --color "1D76DB" --description "arXiv 新论文候选" 2>/dev/null || true
          gh issue create \
            --title "arXiv 候选论文更新 $(date +%F)" \
            --label "new-papers" \
            --body-file "${{ steps.fetch.outputs.report }}"
      - name: Commit seen ids
        if: steps.fetch.outputs.has_new == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/seen_arxiv_ids.txt
          if ! git diff --cached --quiet; then
            git commit -m "chore: update seen arxiv ids"
            git push
          fi
      - name: Report incomplete scan
        if: always() && steps.fetch.outputs.scan_status == 'partial'
        run: |
          echo "::error::扫描不完整。失败组与 RSS 覆盖限制见运行摘要；候选发布状态见前面的步骤。"
          exit 1
